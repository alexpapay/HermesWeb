@model OvpNgDatabase.Models.OvpNg.CompanyProfile

<h5>Добавить новый профиль</h5>
@using (Html.BeginForm("AddCompanyProfile", "Admin", FormMethod.Post))
{
    <table class="table table-striped">
        <tr>
            <th scope="col">Наименование данных</th>
            <th scope="col">Значение</th>
        </tr>
        <tr>
            <td>@Html.DisplayNameFor(model => model.Name)</td>
            <td>@Html.TextBoxFor(companyProfile => companyProfile.Name, new { @class = "form-control" })</td>
        </tr>
    </table>
    <div class="row h-100 justify-content-center align-items-center">
        <button class="btn btn-primary btn-outline-success align-content-center" type="submit">Добавить</button>
    </div>
}